const liveUrl = "https://api.zippyapp.online"
const localUrl = "http://10.0.2.2:3000" // Use 10.0.2.2 for Android Emulator, or localhost / IP for others

export const Config = {
  API_BASE_URL: `${localUrl}/api`
};
